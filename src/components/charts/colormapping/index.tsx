import React from 'react';
import { OpacityContainer } from '../../';

const ColorMapping = () => {
  return (
    <OpacityContainer>
      <div>ColorMapping</div>
    </OpacityContainer>
  );
};

export default ColorMapping;
